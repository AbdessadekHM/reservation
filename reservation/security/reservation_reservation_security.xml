<odoo>
    <record id="reservation_reservation_access_rule_users" model="ir.rule">
        <field name="name">Reservation model access rules for users</field>
        <field name="model_id" ref="reservation.model_reservation_reservation" />
        <field name="domain_force">[('create_uid', '=', user.id)]</field>
        <field name="groups" eval="[4,ref('reservation_groups_users')]"/>

    </record>
</odoo>